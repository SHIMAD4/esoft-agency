import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function UsersPage() {
  return (
    <SafeAreaView className="flex-1 flex-row gap-2">
      <Text>Users</Text>
    </SafeAreaView>
  );
}
